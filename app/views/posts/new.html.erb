<% provide(:title, "新規投稿") %>
<h1 class="page-title">新規投稿</h1>
<div class="row justify-content-center">
  <div class="col-md-6 col-md-offset-3">
    <section class="user-info-newpost">
      <%= image_tag @user.image.url, class: "img-rounded profile-img" %>
      <h1><%= @user.name %></h1>
    </section>
    <%= form_for(@post, url: '/newpost') do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <div class="text-area">
        <%= f.text_area :content, placeholder: "ここにお書きください" %>
      </div>
      <div class="text-center">
        <%= f.submit "投稿する", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>